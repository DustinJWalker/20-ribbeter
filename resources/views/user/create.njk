{% extends "layout" %}

{% block content %}

{{ form.open({action: 'UserController.store'}) }}

{{ csrfField }}


<div class="container">

  <div class="card">
    <div class="Login">
      <h1>Login</h1>
    </div>
      <div class="field">
        {{ form.label('email', 'Enter email address') }}
        {{ form.text('email', null, { class: 'form-control', placeholder: 'Email' }) }}
      </div>

      <div class="field">
        {{ form.label('password', 'Choose a strong password') }}
        {{ form.password('password', null, { class: 'form-control', placeholder: 'Password' }) }}
      </div>

    <div class="button">
        {{ form.submit('Register', null, { class: 'register' }) }}
        {{ form.submit('Login', null, {class: 'login' }) }}
    </div>
  </div>
</div>

{{ form.close() }}



{% endblock %}
