<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>20-ribbeter</title>
    <link href="https://fonts.googleapis.com/css?family=Dosis:300,400,500" rel="stylesheet">
    <link rel="stylesheet" href="/dist/app.css" media="screen" title="no title">
  </head>
  <body>
    <nav class="top__nav">
      <div class="top__nav-title">
        <p>Ribbeter</p>
      </div>
      <ul class="top__nav-items">
        {% if currentUser %}
        <li><a href="/"></a>Ribbets</li>
        {% else %}
        <li><a href="/users"></a>Frogs</li>
        <li><a href="/login"></a>Login</li>
        {% endif %}
      </ul>
    </nav>

    <div class="section">
  <div class="container">
    {% if old('error') %}

    <div class="alert alert-danger">
      <p>{{ old('error') }}</p>

    </div>
    {% endif %}

    {% if old('success') %}

    <div class="alert alert-success">
      <p>{{ old('success') }}</p>

    </div>
    {% endif %}
    {% block content %}


    {% endblock %}
  </div>
</div>






  </body>
</html>
