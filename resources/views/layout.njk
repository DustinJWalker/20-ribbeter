<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>20-ribbeter</title>
    <link href="https://fonts.googleapis.com/css?family=Dosis:300,400,500" rel="stylesheet">
    <link rel="stylesheet" href="/dist/app.css">
  </head>
  <body>
  <div class="page">

    <nav class="navbar">
      <div class="nav__container">

      <div class="nav__title">
        <p>Ribbeter</p>
      </div>
        <div class="nav__items">
          <p><a href="/"></a>Ribbets</p>
          <p><a href="/users"></a>Frogs</p>
          <p><a href="/login"></a>Login</p>
        </div>
      </div>



          {# {% if currentUser %} #}
          {# {% else %} #}
          {# {% endif %} #}
    </nav>

    <div class="section">
  <div class="container">
    {% if old('error') %}

    <div class="alert alert-danger">
      <p>{{ old('error') }}</p>

    </div>
    {% endif %}

    {% if old('success') %}

    <div class="alert alert-success">
      <p>{{ old('success') }}</p>

    </div>
    {% endif %}
    {% block content %}


    {% endblock %}
  </div>
</div>






</div>
  </body>
</html>
